# Zależności do uruchomienia testów backendu.
# Instalacja:  pip install -r tests/requirements-test.txt
pytest>=8,<9
factory_boy>=3.3,<4
Faker>=30
httpx>=0.27,<0.29    # wymagane przez fastapi.testclient (Starlette TestClient); górna granica = zgodność ze starlette 0.37
