{
  "name": "lokalo-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Powłoka natywna (Capacitor) aplikacji Lokalo — Android + iOS. Pakuje zbudowany frontend i łączy się z instancją lokalu po adresie.",
  "scripts": {
    "build:web": "npm --prefix ../frontend run build",
    "sync": "cap sync",
    "android:add": "cap add android",
    "android:sync": "npm run build:web && cap sync android",
    "android:open": "cap open android",
    "ios:add": "cap add ios",
    "ios:sync": "npm run build:web && cap sync ios",
    "ios:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/push-notifications": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
