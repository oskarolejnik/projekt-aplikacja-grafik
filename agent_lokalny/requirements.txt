# Zależności agenta lokalnego (instaluj na serwerze Gastro LSI):
#   pip install -r requirements.txt
SQLAlchemy>=2.0,<3
pymssql>=2.2          # sterownik MS SQL Server (wheels z FreeTDS — bez instalacji ODBC)
requests>=2.28
python-dotenv>=1.0
